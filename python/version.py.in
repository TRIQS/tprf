""" This file is used to construct the python file 'version.py' where all variables like
    @VAR@ are subsituted by cmake.
    The 'version.py' file can then be imported to get version informations (TRIQS and TPRF).
"""

tprf_version = 0.0
tprf_hash = "@TPRF_GIT_HASH@"
tprf_hash_short = tprf_hash[:7]
triqs_version = "@TRIQS_VERSION@"
triqs_hash = "@TRIQS_GIT_HASH@"
triqs_hash_short = triqs_hash[:7]

info = (tprf_version, tprf_hash_short, triqs_version, triqs_hash_short)

